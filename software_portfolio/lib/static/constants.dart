import 'package:flutter/material.dart';

const primaryColor = Colors.white;
const secondaryColor = Color.fromARGB(255, 0, 0, 0);
const darkColor = Color(0xFF191923);
const bodyTextColor = Color(0xFF8B8B8D);
const splashBgColor = Colors.white;
const homeBgColor = Color.fromARGB(255, 58, 73, 81);
const defaultPadding = 20.0;

// Splash welcome messages
List<String> texts = [
  'Waiting for the magic to happen...',
  'Nearly there... Wait a little longer.',
  'Three, two, one... Finally done!'
];
